--- /root/backups/phase5-initial-rollout-freeze-2026-03-25-151728/host-cron/n8n_sync_check.sh	2026-03-25 15:17:28.402284163 +0000
+++ /root/services/webhook-gateway/n8n_sync_check.sh	2026-03-27 00:15:41.644039445 +0000
@@ -8,24 +8,21 @@
 EXPORT_NAME="sync-check-latest"
 CONTAINER_EXPORT="/home/node/.n8n/${EXPORT_NAME}"
 HOST_EXPORT="/root/data/n8n/${EXPORT_NAME}"
-TG_TOKEN="$(cat /root/.openclaw/credentials/telegram-bot-token)"
-TG_CHAT="7756506076"
+SEND_HELPER="${WORKSPACE}/projects/ops/send_chief_telegram.sh"

-send_tg() {
-  curl -s -X POST "https://api.telegram.org/bot${TG_TOKEN}/sendMessage" \
-    -d "chat_id=${TG_CHAT}" \
-    --data-urlencode "text=$1" >/dev/null
+send_alert() {
+  "${SEND_HELPER}" "$1" >/dev/null
 }

 if [ -z "${N8N_CID}" ]; then
-  send_tg "OpenClaw n8n sync-check alert: n8n container not found on $(hostname)."
+  send_alert "OpenClaw n8n sync-check alert: n8n container not found on $(hostname)."
   exit 1
 fi

 rm -rf "${HOST_EXPORT}"

 if ! docker exec "${N8N_CID}" sh -lc "rm -rf '${CONTAINER_EXPORT}' && mkdir -p '${CONTAINER_EXPORT}' && n8n export:workflow --all --pretty --separate --output='${CONTAINER_EXPORT}'" >/tmp/n8n_sync_check_export.log 2>&1; then
-  send_tg "OpenClaw n8n sync-check alert: live export failed on $(hostname) at $(date -Is)."
+  send_alert "OpenClaw n8n sync-check alert: live export failed on $(hostname) at $(date -Is)."
   cat /tmp/n8n_sync_check_export.log
   exit 1
 fi
@@ -147,7 +144,7 @@
 n8n sync-check alert snapshot
 Created: $(date -Is)
 TXT
-  send_tg "OpenClaw n8n sync-check alert on $(hostname): ${REPORT}"
+  send_alert "OpenClaw n8n sync-check alert on $(hostname): ${REPORT}"
   exit 1
 fi
