---
summary: "Evidence-backed inventory of the clearly broken, incomplete, or not-yet-proven surfaces from the final human/operator validation pass."
title: "Final Broken Surface Diagnostic"
---

# Final Broken Surface Diagnostic

Validation baseline reviewed:

- Main session logs under `.openclaw/agents/main/sessions/`
- native cron run logs under `.openclaw/cron/runs/`
- operator-review prep and synced artifacts under workspace archives and generated-current outputs
- `docs/projects/deployment-topology/final-human-validation-results.md`
- current live runtime container logs and runtime config
- direct `model-memory` database evidence

## Surface inventory

| Surface                                                         | Classification   | Current evidence                                                                                                                                                                                                                                                                                                                                                                      | Exact failure mode                                                           | Likely root cause                                                                                                                                                | Required fix path                                                                                                                                                                                                  | Required rerun proof                                                                                         |
| --------------------------------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------ |
| bootstrap/startup message visibility                            | `pass`           | Bare `/new` was rerun after the startup-context patch; the visible answer stayed a normal greeting and did not leak the bootstrap block.                                                                                                                                                                                                                                              | No current failure reproduced after the fix.                                 | User-visible startup text previously included internal runtime bootstrap context because bare-session startup appended the prelude into the visible prompt body. | Fixed in `src/auto-reply/reply/get-reply-run.ts` by moving startup context back into system-only prompt material.                                                                                                  | Live `/new` greeting stays clean on the rebuilt runtime.                                                     |
| browser tool path availability                                  | `pass`           | Live browser proof succeeded with `Open https://example.com in the browser tool and tell me the page title only.` and returned `Example Domain` through the actual browser path.                                                                                                                                                                                                      | No current failure reproduced after the fix.                                 | Relative host-browser requests were dispatching through the wrong local path instead of the healthy loopback browser-control service.                            | Fixed in `extensions/browser/src/browser/client-fetch.ts` plus startup timeout and stale-profile recovery changes under `extensions/browser/src/browser/chrome.ts` and `extensions/browser/src/browser/client.ts`. | Browser tool path remains healthy on rebuilt runtime.                                                        |
| managed host browser profile start                              | `pass`           | Runtime now recovers from stale managed-profile lock state and starts the browser path cleanly after rebuild.                                                                                                                                                                                                                                                                         | No current failure reproduced after the fix.                                 | Stale Chromium singleton/profile locks blocked managed host browser startup.                                                                                     | Fixed with one-shot managed-profile reset/retry on recoverable stale-lock stderr.                                                                                                                                  | Rebuilt runtime starts browser without manual profile cleanup.                                               |
| web-researcher browser-required reads                           | `incomplete`     | The original failing evidence is now stale because it predates the browser fix, but this exact delegated browser-only read was not rerun in this pass.                                                                                                                                                                                                                                | Browser-backed delegated research is not freshly re-proven in this pass.     | The original failure was downstream of the broken browser path, which is now fixed.                                                                              | Rerun one browser-required `web-researcher` task during the next human prompt pass.                                                                                                                                | Browser-only delegated read succeeds without fetch fallback.                                                 |
| delegated research completion                                   | `incomplete`     | The earlier delegated comparison prompt still has no recorded final answer in the old session log.                                                                                                                                                                                                                                                                                    | No fresh final delegated answer was recorded in this pass.                   | The earlier run ended before completion; that evidence is stale rather than current-product truth.                                                               | Rerun the delegated comparison prompt during the remaining human prompt pass.                                                                                                                                      | Completed delegated answer with sources.                                                                     |
| researcher specialist-pack true routing                         | `incomplete`     | The live allowlist now includes `researcher`, but this exact Main-to-Researcher prompt was not rerun after the config fix.                                                                                                                                                                                                                                                            | Current routing state is not freshly re-proven from the live prompt path.    | The earlier failure was the live `tools.agentToAgent.allow` denial; that live config issue is now fixed.                                                         | Keep the rerun in the remaining human prompt pack instead of calling the stale failure current.                                                                                                                    | Researcher-specialist prompt routes successfully without Main substituting itself.                           |
| model-memory prompt capture proof                               | `pass`           | DB-backed live proof session `codex-live-mm-write-diag-1776435267602` created `model_memory.sources`, `model_memory.memory_objects`, `model_memory.memory_support_items`, and `model_memory.write_events` rows with current timestamps; the write event id was `140bf769-8248-5f61-9fe1-2f18e1ee7a0a`.                                                                                | No current failure on the live write path for a stable preference capture.   | The earlier proof seam was weakened by epoch default timestamps and transcript-only judgment.                                                                    | Fixed timestamp defaults in `extensions/model-memory/src/db/database-memory-object-store.ts` and verified with a live write-path diagnostic.                                                                       | Live ordinary-turn capture produces a current-timestamp write event.                                         |
| model-memory prompt retrieval proof                             | `pass`           | DB-backed retrieval proof session `codex-live-mm-retrieval-diag-1776435619123` persisted `runtime_context.retrieval_requests`, `runtime_context.retrieval_result_sets`, and `runtime_context.retrieval_result_items`; the top selected object was the newly written preference object `9d89ef85-f14e-5677-8970-5e7a403632d0`, and the retrieval pack was included in the context run. | No current failure on the retrieval/context path for that preference memory. | The earlier human test relied on answer text without retrieval/runtime evidence.                                                                                 | Proved via live retrieval/context diagnostic against the rebuilt runtime and live DB.                                                                                                                              | Retrieval rows and context-run artifacts show the expected preference object selected for context.           |
| daily-summary canary / retrieval proof                          | `incomplete`     | The earlier failure remains true only in the narrow sense that no fresh canary was seeded and re-proven in this pass.                                                                                                                                                                                                                                                                 | Daily-summary recovery is still not freshly re-proven.                       | No finalized canary was injected into the daily-summary lane during this pass.                                                                                   | Keep in the remaining human-memory-soak prompt list rather than misclassifying it as a fixed runtime regression.                                                                                                   | A canary line is written to the finalized daily summary and later retrieved via `daily_continuity` evidence. |
| daily operator review current-day chain                         | `pass`           | Native daily cron run, repo-owned prep, and sync are aligned again; host log now records `daily operator review artifact synced: /root/.openclaw/workspace/archives/daily_operator_reviews/2026-04-17.md`.                                                                                                                                                                            | No current failure reproduced after the rerun.                               | Earlier validation happened before the fresh native daily run existed.                                                                                           | Rerun completed; current-day artifact body now exists.                                                                                                                                                             | Fresh daily artifact body exists for `2026-04-17`.                                                           |
| weekly operator review content freshness                        | `pass`           | Fresh weekly native run, sync, and preview now align; host log records `weekly operator review artifact synced: /root/.openclaw/workspace/archives/weekly_operator_reviews/2026-W16.md` and `preview-only: summary generated .../weekly_review_telegram_summary_current.txt`.                                                                                                         | No current stale-output failure reproduced after the rerun.                  | The synced weekly artifact had been reflecting an older run.                                                                                                     | Fixed by rerunning the native weekly review, then syncing and previewing against the latest successful run.                                                                                                        | Synced weekly artifact and preview match the latest native run.                                              |
| weekly maintenance debt guard fresh proof                       | `pass`           | Fresh native maintenance run at `2026-04-17 13:43:35 UTC` returned a bounded debt report instead of unreadable-path failure.                                                                                                                                                                                                                                                          | No current failure reproduced after the payload-path correction.             | The job briefly pointed Chief at `/app/docs/...` paths that were not readable from the dedicated session surface.                                                | Restored workspace-visible maintenance inputs in live cron payload and reran the native job.                                                                                                                       | Fresh maintenance run reports only the bounded open debt item `MD-013`.                                      |
| browser / gateway CLI direct call path inside runtime container | `runtime_broken` | `openclaw browser ...` and `openclaw gateway call health --json` hang inside the container while direct loopback HTTP succeeds.                                                                                                                                                                                                                                                       | CLI control path is unreliable in the live container.                        | Likely shared gateway/tool dispatch issue for local CLI calls.                                                                                                   | Avoid using the hanging CLI path for browser validation; inspect separately if still needed after browser host path fix.                                                                                           | Targeted direct proof that required browser and cron flows no longer depend on the hanging CLI path.         |

## Remaining open items

1. rerun one delegated `web-researcher` browser-only task now that the browser lane is fixed
2. rerun the generic `Researcher:` specialist-route prompt after the allowlist fix
3. seed and verify the daily-summary canary / `daily_continuity` proof lane

## Notes on rigor

- Answer text alone is not treated as a pass for `model-memory` capture or retrieval.
- Workspace daily-memory edits are not treated as `model-memory` proof.
- Weekly operator-review mechanical script success is not treated as a pass unless the synced artifact and preview are refreshed against the latest native run.
- The browser lane is not counted as passing unless the browser tool path itself succeeds where fetch fallback is insufficient.
