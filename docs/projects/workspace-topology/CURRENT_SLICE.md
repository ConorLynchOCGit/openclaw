---
summary: "Active slice for the workspace-topology project."
title: "Workspace Topology Current Slice"
---

# Workspace Topology Current Slice

## Active slice

`slice_3_runtime_assembly_and_topology_enforcement`

## Goals

1. assemble runtime-facing compatibility files from canonical durable sources
2. activate the bootstrap-file registry in repo code
3. preserve the intended memory architecture inside assembled `MEMORY.md`
4. wire topology/bootstrap checks into the default repo verification path
5. extract deployment-topology into its own canonical project workspace
6. make bootstrap seed timing explicit in the registry
7. keep runtime repo-import parity directly checkable
8. queue canonical path resolution and runtime arbitration as the immediate
   post-document-ingest hardening slice
9. explicitly reconcile canonical repo project workspaces versus lingering
   workspace project paths
10. convert legacy workspace project packs into canonical import-backed aliases
    wherever runtime-generated writable state is not required
11. keep only the narrow justified writable compatibility surfaces, starting
    with `workspace/projects/ops/generated_current/`

## Exit criteria

- `docs/system/` exists with the core pointer/control surfaces
- registries exist
- the topology rules are recorded durably
- migration candidates are classified, not guessed
- project pack compliance is known
- initial deterministic checks run successfully
- roadmap-like detail has a canonical home
- runtime compatibility file ownership is explicitly mapped
- memory-layer preservation is recorded clearly
- runtime compatibility files are assembled through a real repo path
- bootstrap-file registry materially affects assembly or validation
- topology/bootstrap enforcement is part of the default repo gate
- the next topology hardening slice is explicitly defined for canonical
  path-resolution, runtime arbitration, and read/write ownership
- runtime project-surface ownership is written down clearly enough to prevent
  “two project systems” drift
- legacy workspace project names resolve back to the canonical repo-owned
  project tree rather than acting as a parallel registry
