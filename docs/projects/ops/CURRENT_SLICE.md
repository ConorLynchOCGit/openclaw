# Current Slice

- keep `docs/projects/ops/` as the canonical ops project pack
- keep `workspace/projects/ops/generated_current/` as the narrow writable compatibility surface for live report artifacts
- keep high-value operator artifacts in `generated_current/` as explicit current aliases with provenance back to their canonical owner instead of letting them become hand-edited source files
