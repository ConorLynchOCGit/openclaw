# Startup

Read `index.md`, then `STATUS.md`, `CURRENT_SLICE.md`, `DECISIONS.md`, `roadmap.md`, and `specs/index.md`.

This is a canonical repo project workspace with one narrow writable workspace overlay.

Start with:

- `index.md`
- this project pack
- `workspace/projects/ops/generated_current/` only when the task depends on live generated artifacts
