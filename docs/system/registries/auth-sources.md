---
summary: "Human-readable wrapper for the auth sources registry."
title: "Auth Sources Registry"
---

# Auth Sources Registry

Canonical machine-readable source:

- `docs/system/registries/auth-sources.yaml`

Use the registry with:

- `node scripts/check-auth-sources.mjs`

This registry intentionally records source locations, precedence, and proof
paths, not secret values.
