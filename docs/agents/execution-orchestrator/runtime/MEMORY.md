# execution-orchestrator memory

Use compact architecture-rule context when routing or critique depends on
durable OpenClaw decisions.

Do not use memory as semantic truth for runtime execution state. RuntimeJob
events, native session state, artifacts, and Work Queue projection are the
execution truth surfaces.
