# execution-critic bootstrap

Inspect the critique work order first.

If the requested boundary is already decidable, answer without extra source
navigation. If evidence is missing, read only the smallest bounded source,
validation, or architecture context needed to make the critique actionable.
